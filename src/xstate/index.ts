import type { AgentMachine } from '../types.js';

/**
 * Convert an agent machine to an XState machine definition
 * for visualization in the Stately Editor.
 * TODO: implement
 */
export function toXStateMachine(_machine: AgentMachine): unknown {
  throw new Error('toXStateMachine is not yet implemented');
}
