{
  "name": "crash-recovery",
  "title": "Resume a run that crashed mid-way",
  "kind": "agent-workflow",
  "origin": {
    "type": "stately-agent",
    "source": "Events-only resume: recover a crashed run from its persisted event log alone",
    "url": null
  },
  "comparison": {
    "targets": ["LangGraph checkpointer"],
    "purpose": "Writes an article in two steps while logging every result, then restarts from the log alone: finished steps replay instantly and only the interrupted one runs again."
  },
  "starters": ["state machines", "the history of the fax machine", "why builds are slow"]
}
