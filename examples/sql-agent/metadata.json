{
  "name": "sql-agent",
  "title": "SQL Agent",
  "kind": "pattern",
  "origin": {
    "type": "pattern",
    "source": "Approval-gated SQL / query agent pattern",
    "url": "https://github.com/langchain-ai/langgraphjs/tree/main/examples"
  },
  "comparison": {
    "targets": ["sql-agent", "human-in-the-loop"],
    "purpose": "An approval-gated query flow over an in-memory table: the model plans a query, a human approves via typed interaction meta, and a local engine executes it."
  }
}
