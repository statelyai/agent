{
  "name": "hono-host",
  "title": "Hono Host",
  "kind": "host-adapter",
  "manual": true,
  "origin": {
    "type": "platform-adapter",
    "source": "Hono route handlers calling runAgent, typechecked against hono",
    "url": "https://hono.dev/"
  },
  "comparison": {
    "targets": ["Hono", "edge agent backends"],
    "purpose": "Controlled mode over HTTP the Hono way: idle/done + in-memory snapshot store, plus a streaming endpoint that pipes runAgent's onChunk into the response body."
  }
}
