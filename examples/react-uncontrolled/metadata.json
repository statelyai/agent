{
  "name": "react-uncontrolled",
  "title": "React (Uncontrolled)",
  "kind": "host-adapter",
  "manual": true,
  "origin": {
    "type": "platform-adapter",
    "source": "React component owning the actor lifecycle via provideExecutors + createActor; typechecked against react + @types/react (no @xstate/react — hand-rolled useSyncExternalStore)",
    "url": "https://react.dev/"
  },
  "comparison": {
    "targets": ["React", "@xstate/react"],
    "purpose": "Uncontrolled mode: createActor(provideExecutors(machine, executors)) drives itself, React observes snapshots and sends user events, and onChunk streams text into the UI."
  }
}
