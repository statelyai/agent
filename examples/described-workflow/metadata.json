{
  "name": "described-workflow",
  "title": "Described Workflow (state interpretation)",
  "kind": "agent-workflow",
  "origin": {
    "type": "stately-agent",
    "source": "New example for runAgent's `getRequests` option",
    "url": null
  },
  "comparison": {
    "targets": ["LangGraph", "hand-rolled loop"],
    "purpose": "A plain XState machine with zero invokes — prompts live in state descriptions/meta — runs unmodified as an agent via runAgent({ getRequests }), with the message log aggregated by the runtime onto snapshot.messages."
  },
  "starters": []
}
