{
  "name": "cloudflare-agent-host",
  "title": "Cloudflare Agents Host",
  "kind": "host-adapter",
  "manual": true,
  "origin": {
    "type": "platform-adapter",
    "source": "Cloudflare Agents integration; a workspace package with a real wrangler project, booted locally with `wrangler dev` and tested in workerd via @cloudflare/vitest-plugin",
    "url": "https://developers.cloudflare.com/agents/"
  },
  "comparison": {
    "targets": ["Cloudflare Agents", "durable agent runtimes"],
    "purpose": "Persists XState actor snapshots in Durable Object state, so a conversation survives eviction; keyless scripted executors by default, live AI SDK models when OPENAI_API_KEY is bound."
  },
  "starters": []
}
