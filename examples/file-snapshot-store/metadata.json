{
  "name": "file-snapshot-store",
  "title": "Application-owned snapshot storage and actor lifetime",
  "kind": "host-adapter",
  "origin": {
    "type": "stately-agent",
    "source": "Native XState snapshot persistence and createActor integration in application code",
    "url": null
  },
  "comparison": {
    "targets": ["Redis", "Postgres", "KV", "filesystem storage", "XState createActor"],
    "purpose": "Two halves of one axis: persisting and resuming native XState snapshots through ordinary file I/O, and keeping a single long-lived actor alive across the same run with nothing persisted. Neither needs an Agent-specific storage adapter or actor supervisor."
  },
  "runners": [
    {
      "label": "Persist to disk, resume, then keep one actor alive",
      "export": "runFileSnapshotStoreDemo"
    }
  ]
}
