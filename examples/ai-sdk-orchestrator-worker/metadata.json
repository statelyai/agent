{
  "name": "ai-sdk-orchestrator-worker",
  "title": "Plan a feature, then write the file changes",
  "kind": "comparison",
  "origin": {
    "type": "docs",
    "source": "Vercel AI SDK orchestrator-worker workflow",
    "url": "https://ai-sdk.dev/docs/agents/workflows#orchestrator-worker"
  },
  "comparison": {
    "targets": ["Vercel AI SDK", "orchestrator-worker"],
    "purpose": "An orchestrator plans which files a small feature touches, then a worker writes the change for each file — the result reads as a plan plus one line per changed file."
  },
  "starters": [
    "Add a dark mode toggle persisted to localStorage.",
    "Add a CSV export button to the reports page.",
    "Add a rate limit to one public API route."
  ]
}
