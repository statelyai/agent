{
  "name": "express-host",
  "title": "Express Host",
  "kind": "host-adapter",
  "manual": true,
  "origin": {
    "type": "platform-adapter",
    "source": "Express route handlers calling runAgent, typechecked against express + @types/express",
    "url": "https://expressjs.com/"
  },
  "comparison": {
    "targets": ["Express", "REST agent backends"],
    "purpose": "Controlled mode over HTTP: POST /agent runs a machine to idle/done, an in-memory snapshot store keyed by run id, and POST /agent/:id/resume delivers the human event."
  }
}
