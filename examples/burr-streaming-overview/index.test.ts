import { test } from 'vitest';
import { runBurrStreamingOverviewExample } from './index.js';

test('streaming-overview router keeps safety and mode as explicit states', runBurrStreamingOverviewExample);
