{
  "name": "ai-sdk-parallel-review",
  "title": "Review code from three angles at once",
  "kind": "comparison",
  "origin": {
    "type": "docs",
    "source": "Vercel AI SDK parallel processing workflow",
    "url": "https://ai-sdk.dev/docs/agents/workflows#parallel-processing"
  },
  "comparison": {
    "targets": ["Vercel AI SDK", "parallel processing"],
    "purpose": "Security, performance, and maintainability reviewers read the same code concurrently, then one summary combines their findings."
  },
  "starters": [
    "function login(u, p) { return db.query('SELECT * FROM users WHERE u=' + u) }",
    "const dedupe = a => a.filter((x, i) => a.indexOf(x) === i)",
    "app.get('/file', (req, res) => res.sendFile('/data/' + req.query.name))"
  ]
}
